name: AppImager
description: AppImager manages AppImage dependencies, assists in the creation of AppDir's and creates AppImages from source code

build: ./build.sh

integration: bin/AppImager.sh

base: 12.04

repositories:
    - "ppa:fkrull/deadsnakes"

require_build:
    - python3.5
